<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Inherit Form View to Modify it -->
        <record id="res_users_inherit_subcontractor_id" model="ir.ui.view">
            <field name="name">res.users.inherit.subcontractor</field>
            <field name="model">res.users</field>
            <field name="inherit_id" ref="base.view_users_form"/>
            <field name="arch" type="xml">
                <xpath expr="//group[@name='preferences']" position="inside">
                    <!-- Add your fields or attributes here -->
                    <field name="managers_ids" widget="many2many_tags"/>
                </xpath>

            </field>
        </record>

      </data>
</odoo>