<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="workflow_form_view_id" model="ir.ui.view">
            <field name="name">workflow.form.view</field>
            <field name="model">workflow</field>
            <field name="arch" type="xml">
                <form string="">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="company_id" invisible="1"/>
                            <field name="construction_project_id"/>
                            <field name="model_id"/>
                            <field name="stage_ids"/>
                        </group>
                    </sheet>
<!--                    <div class="oe_chatter">-->
<!--                        <field name="message_follower_ids" widget="mail_followers"/>-->
<!--                        <field name="activity_ids" widget="mail_activity"/>-->
<!--                        <field name="message_ids" widget="mail_thread"/>-->
<!--                    </div>-->
                </form>
            </field>
        </record>


        <record id="workflow_tree_view_id" model="ir.ui.view">
            <field name="name">work.flow.tree.view</field>
            <field name="model">workflow</field>
            <field name="arch" type="xml">
                <list>
                    <field name="name"/>
                    <field name="company_id" invisible="1"/>
                    <field name="construction_project_id"/>
                    <field name="model_id"/>
                    <field name="stage_ids" widget="many2many_tags"/>
                </list>
            </field>
        </record>

        <record id="workflow_action_id" model="ir.actions.act_window">
            <field name="name">Workflow</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">workflow</field>
            <field name="view_mode">list,form</field>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    <!-- Add Text Here -->
                </p>
                <p>
                    <!-- More details about what a user can do with this object will be OK -->
                </p>
            </field>
        </record>


        <!-- This Menu Item must have a parent and an action -->
        <menuitem id="workflow_menu_id" name="Workflow" parent="smac_construction_management.configuration_menu_id"
                  action="workflow_action_id" sequence="0"/>

    </data>
</odoo>