<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.construction</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="15"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app string="Construction" name="construction">
                    <block title="Stages" name="construction_stage_settings">
                        <setting string="Stage Policy"
                                 help="Choose whether overdue approvals move to the next stage or send a notification.">
                            <field name="stage_policy"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_construction_config_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module': 'construction'}</field>
    </record>

    <menuitem id="menu_construction_config_settings"
              name="Settings"
              parent="smac_construction_management.spm_menu_categ_id"
              action="action_construction_config_settings"
              sequence="99"/>
</odoo>
